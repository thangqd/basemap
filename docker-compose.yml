version: '3.8'

services:
  styles-server:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./styles:/usr/share/nginx/html:ro
    restart: unless-stopped
    container_name: openmaptiles-styles